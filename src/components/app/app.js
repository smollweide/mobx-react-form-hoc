import React from "react";
import LoginForm from "../login-form/login-form";

const App = () => {
    return (
        <div>
            <h2>mobx-react-form-hoc demo</h2>
            <LoginForm />
        </div>
    );
};

App.displayName = "App";

export default App;
